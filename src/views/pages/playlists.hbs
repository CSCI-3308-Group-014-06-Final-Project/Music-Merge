<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    /* Background and text color changes */
    body {
      background-color: #121212; /* Dark background */
      color: #FFFFFF; /* White text for contrast */
    }

    h1 {
      color: #1DB954; /* Spotify green for the title */
    }

    .table {
      background-color: #282828; /* Dark table background */
      color: #FFFFFF; /* White text */
    }
    
    /* Table row hover effect */
    tr:hover {
      background-color: #404040; /* Darker shade on hover */
    }
  </style>
</head>
<body>
  <div class="container mt-4">
    <h1 class="text-center">Your Merged Playlists</h1>
    <table class="table">
      <thead>
        <tr>
          <th>Playlist Name</th>
          <th>Link to Playlist</th>
        </tr>
      </thead>
      <tbody>
        {{#each playlists}}
        <tr> <!-- Start a new row for each playlist -->
          <td>{{name}}</td> <!-- Display playlist name in the first column -->
          <td><a href="{{link}}">Link</a></td> <!-- Display link to playlist in the second column -->
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>

</body>
</html>

